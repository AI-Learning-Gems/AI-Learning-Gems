# Core Scientific Computing
numpy==2.2.6
scipy==1.15.3
pandas==2.2.3

# Visualization
matplotlib==3.10.3
hvplot==0.11.3
holoviews==1.20.2
graphviz==0.21

# Deep Learning (install separately if needed)
# torch  # Not in base env, install via: pip install torch

# Bayesian Statistics (optional, for statistics chapters)
arviz==0.23.1
# pymc  # Not in base env, install via: pip install pymc
statsmodels==0.14.4

# Jupyter/Quarto Integration  
jupyter==1.1.1
ipykernel==6.29.5
ipywidgets==8.1.8

# Content Extraction & Conversion (for source fetching workflow)
requests==2.32.5
beautifulsoup4==4.14.3
markdownify==1.2.2
pillow==10.4.0
PyYAML==6.0.3
tqdm==4.67.2

# Mistral OCR (PDF to Markdown conversion)
mistralai==1.11.1
python-dotenv==1.2.1

# Authenticated Web Extraction (JS-heavy / login-gated pages)
crawl4ai>=0.8.0

# Optional: Install separately if needed
# marker-pdf  # Local PDF→MD: pip install marker-pdf
# trafilatura  # Web content extraction: pip install trafilatura

# Chat Extensions Backend
fastapi>=0.109.2
uvicorn>=0.27.1
openai>=1.55.0
litellm>=1.53.1